# Frontend Environment Configuration

# API Base URL
# In development: Leave empty for relative paths (proxy via Vite)
# In production: Set to the full API server URL (e.g., https://api.example.com)
# Can also be injected at runtime via window.__API_BASE_URL__
VITE_API_BASE_URL=
